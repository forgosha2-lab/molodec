# TODO: Add Welcome Endpoint with Logging

## Steps to Complete

- [x] Create server/logger.js utility file for logging request metadata (method and path).
- [x] Edit server.js to import and initialize the logger.
- [x] Add GET /api/welcome route in server.js that logs the request and returns JSON welcome message.
- [x] Test the server by running it and verifying the /api/welcome endpoint works and logs correctly.
